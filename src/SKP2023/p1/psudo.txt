1번
브루트 포스, 문자열

배열 두개에 각각 저장
시간은 분으로 치환해서 계산하기 편하게 만들고

현재 시간보다 작은 값은 빼버리고 큰 것들만 비교하면서 그 인덱스의 값을 합산
합산한 값이 k와 같거나 넘으면 시작 인덱스와 넘은 시점의 인덱스를 저장
스케쥴 시간 배열에 인덱스차이로 시간 계산


1. 스케쥴을 시간과 개수 배열 두 개로 변환
2. 시간 배열을 분 단위로 치환