
매번 검사할 때 마다 열의 개수만큼 스트링 배열을 만들기
배열의 각 원소 전부를 이분 탐색으로 같은 배열에 있는지 검사 -> O(NlogN)
  - list에 담아서 contains 쓰면 안되나
최악의 경우, 이를 행의 개수만큼 해야됨
1000 * 1000 * log1000
1000000 * 32 = 3천만

해시는 어따 쓰지

  1. 스트링 배열에 담아주고 charAt으로 세로 배열을 리스트에 담아주기
  2. 비교하기
  3. 중복안되면 맨 위의 열 지우고(복사, 자르기) 다시 세로 배열 만들기
  4. 반복

   배열을 새로 안만들고 인덱스만 조정해서 리스트 만들면 될 듯
   자신을 빼고 어떻게 비교를 하지. 중복검사를 해서 개수가 줄어들면 중복이 된 것.
   그러면 이 때 해쉬맵에 삽입해서 크기 차이가 나는지 검사

   3중 for문에서 시간 초과가 나는거 같은데 어떻게 하나씩 작아지는 배열을 빠르게 만들지
   이분 탐색을 여기서 인덱스로 만드나?
   정확히  mid값을 그냥 cnt값으로 출력하면 답이 되려나

   배열 만드는 것을 간단하게 해야함
   90도 회전
   dobarz
   adatak
   ->
   d a
   o d
   b a
   a t
   r a
   z k

   하고 비교